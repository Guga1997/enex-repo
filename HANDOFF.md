# სესიის გაგრძელება სხვა კომპიუტერზე

ეს ფაილი შეიცავს ყველაფერს, რაც სხვა კომპიუტერზე მუშაობის გასაგრძელებლად სჭირდება
როგორც შენ, ისე Claude-ს. თარიღი: 2026-08-29.

---

## 1. კოდის გადატანა

პროექტი OneDrive-შია (`site design and build`), ანუ თავისით დასინქრონდება —
**გარდა ერთი პრობლემისა:** `node_modules` არის 711 MB და ათიათასობით ფაილი.
OneDrive ამაზე დაიხრჩობა.

**გააკეთე ასე:**

1. ამ კომპიუტერზე OneDrive-ის პარამეტრებში გამორთე `node_modules` საქაღალდის სინქი
   (ან უბრალოდ წაშალე იგი — ქვემოთ ისედაც თავიდან დაყენდება).
2. სხვა კომპიუტერზე დაელოდე სინქის დასრულებას (პროექტი `node_modules`-ის გარეშე
   მხოლოდ **696 KB**-ია, 69 ფაილი — წამებში გადავა).

**ალტერნატივა — git:** თუ OneDrive-ს არ ენდობი, პროექტში `.gitignore` უკვე მზადაა
(`node_modules`, `.next`, `.env`, `*.db` გამორიცხულია):

```bash
git init && git add -A && git commit -m "ონლაინ მაღაზია — საწყისი ვერსია"
```

შემდეგ push კერძო რეპოზიტორიაში და clone მეორე მანქანაზე.

> `.env` და `prisma/dev.db` **არ** გადადის git-ით. `.env`-ს ხელით გადაიტან
> (შიგთავსი ქვემოთ, სექცია 4), ბაზას კი ერთი ბრძანებით აღადგენ.

---

## 2. გაშვება ახალ კომპიუტერზე

წინაპირობა: Node.js 20+ (აქ 24.19.0-ზე შენდებოდა).

```bash
npm install
npm run db:push
npm run db:seed
npm run dev
```

- საიტი: http://localhost:3000
- ადმინი: http://localhost:3000/admin
- **ადმინის მონაცემები:** `admin@example.ge` / `admin12345`

`db:seed` ქმნის ადმინს, 3 მთავარ კატეგორიას 10 ქვეკატეგორიით და 27 სადემონსტრაციო
პროდუქტს. თუ ბაზა უკვე გაქვს, seed უსაფრთხოა — `upsert`-ებით მუშაობს.

**API გასაღები თავიდან უნდა შექმნა** — ისინი მხოლოდ ჰეშირებულად ინახება და
ძველის აღდგენა შეუძლებელია. ადმინი → API გასაღებები → შექმნა.

---

## 3. რა არის უკვე გაკეთებული

პროექტი **დასრულებული და გატესტილია**. `npx next build` სუფთად გადის.

| ბლოკი | მდგომარეობა |
|---|---|
| კატალოგი + ფასეტური ფილტრი | ✅ გატესტილი ბრაუზერში |
| ნაშთის ავტომატური წარწერები | ✅ გატესტილი |
| კალათა → checkout → გადახდა | ✅ სრული ნაკადი გატესტილი |
| ნაშთის ჩამოწერა გადახდაზე | ✅ გატესტილი (14 → 13) |
| ადმინ პანელი | ✅ გატესტილი |
| სტოკის API (7 endpoint) | ✅ curl-ით გატესტილი |
| BOG ეკვაირინგი | ⚠️ კოდი მზადაა, სატესტო რეჟიმშია |

### გატესტილი ნაკადის დეტალები

- შეკვეთა `ORD-2026-00001` — 729₾, სტატუსი `PAID`
- პროდუქტ `03904`-ის ნაშთი გადახდის შემდეგ ავტომატურად 14 → 13
- API-დან შექმნილი პროდუქტი მაშინვე გამოჩნდა კატალოგში, ბრენდი — ფილტრში
- ავტორიზაციის გარეშე API აბრუნებს 401

### რაც **არ** გაკეთებულა (შეგნებულად, არა შეცდომით)

1. **შეკვეთის დადასტურების ელფოსტა** — არც მომხმარებელს, არც ადმინს არ მიდის
   შეტყობინება. საჭიროა Resend ან SendGrid.
2. **მომხმარებლის ანგარიშები** — შესვლა/რეგისტრაცია არ არის. შეკვეთა სტუმრადაც
   ფორმდება, ძველი შეკვეთის ნახვა კი ნომრით + ელფოსტით.
3. **Rate limiting** `/api/orders`-ზე — ბოტს შეუძლია შეკვეთების დაგენერირება.
4. **ტესტები** — ავტომატური ტესტები არ დაწერილა; შემოწმება ხელით ჩატარდა.

---

## 4. `.env`-ის შიგთავსი

git-ით არ გადადის. ხელით შექმენი პროექტის ფესვში:

```env
DATABASE_URL="file:./dev.db"

# პროდაქშენზე აუცილებლად შეცვალე — შემთხვევითი 32+ სიმბოლო
AUTH_SECRET="change-me-in-production-min-32-chars-long-secret"

NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- საქართველოს ბანკი (BOG) ---
BOG_CLIENT_ID=""
BOG_CLIENT_SECRET=""
# 1 = სატესტო რეჟიმი, ბანკის გარეშე
PAYMENT_MOCK="1"
```

---

## 5. მიღებული გადაწყვეტილებები

Claude-ს ეს კონტექსტი სჭირდება, რომ იგივე კითხვები აღარ დასვას:

- **პლატფორმა:** საკუთარი კოდი Next.js-ზე (არა Shopify/WooCommerce) — შენი არჩევანი
- **გადახდა:** BOG/TBC ონლაინ ეკვაირინგი
- **სტოკის API:** კონკრეტული სისტემა ჯერ არჩეული არ არის — ამიტომ აშენდა
  **უნივერსალური REST API გასაღებებით**, რომელსაც ნებისმიერი მხარე მიუერთდება
- **ეტალონი:** https://www.intellcom.ge/ — ფილტრის მოდელი და ნაშთის წარწერები
  სწორედ იქიდან აღებულია
- **ბაზა:** SQLite დეველოპმენტისთვის, PostgreSQL პროდაქშენისთვის
  (`schema.prisma`-ში ერთი ხაზი იცვლება)
- **ენა:** მთელი ინტერფეისი ქართულად; კოდის კომენტარებიც ქართულად

---

## 6. სად რა დევს

```
prisma/schema.prisma            მონაცემთა მოდელი (9 ცხრილი)
prisma/seed.ts                  საწყისი მონაცემები

src/lib/catalog.ts              ⭐ ფილტრი, ფასეტები, რაოდენობები — გულია
src/lib/stock.ts                ნაშთის წარწერები ("დარჩენილია N ცალი")
src/lib/orders.ts               გადახდა + ნაშთის ჩამოწერა (იდემპოტენტური)
src/lib/payments/bog.ts         BOG ეკვაირინგი + mock რეჟიმი
src/lib/api-auth.ts             API გასაღებების ავთენტიფიკაცია
src/lib/auth.ts                 ადმინის სესია (JWT cookie)

src/app/catalog/[[...slug]]/    კატალოგი — კატეგორია + ფილტრი
src/app/product/[slug]/         პროდუქტის გვერდი
src/app/checkout/               შეკვეთის გაფორმება
src/app/order/[id]/             შეკვეთის სტატუსი
src/app/admin/                  ადმინ პანელი (6 გვერდი + actions.ts)
src/app/api/v1/                 ⭐ სტოკის API გარე სისტემისთვის
src/app/api/orders/             შეკვეთის შექმნა (ფასები სერვერზე მოწმდება)
src/app/api/payments/bog/       ბანკის callback

src/components/FilterSidebar.tsx  ფილტრის UI
src/components/CartProvider.tsx   კალათა (localStorage)
```

---

## 7. შემდეგი ნაბიჯები

**პროდაქშენამდე სავალდებულო (სამი რამ):**

1. **PostgreSQL** — `prisma/schema.prisma`-ში `provider = "postgresql"`,
   `DATABASE_URL` შეცვალე, `npx prisma migrate deploy`. SQLite ერთდროულ
   ჩაწერაზე არ ვარგა.
2. **სურათების საცავი** — ახლა `public/uploads`-შია, რაც Vercel-ზე ან
   რამდენიმე ინსტანსზე არ იმუშავებს. S3 / Cloudflare R2 — მხოლოდ
   `src/app/api/admin/upload/route.ts` შეიცვლება.
3. **`.env`** — `AUTH_SECRET` შემთხვევითი, ადმინის პაროლი შეცვლილი.

**BOG-ის ჩართვა:** ბანკიდან `client_id` / `client_secret` → `.env` →
`PAYMENT_MOCK=0`. ბანკს მიაწოდე callback: `https://შენი-დომენი.ge/api/payments/bog/callback`.

⚠️ `BOG_PUBLIC_KEY`-ის გარეშე callback უარყოფილი იქნება და შეკვეთა გადახდილად
**არ** მოინიშნება. ეს განზრახაა — ხელმოწერის შემოწმების გარეშე ნებისმიერს
შეუძლია ყალბი „გადახდილია" გამოგზავნა.

---

## 8. Claude-სთვის: გასაგრძელებელი პრომპტი

ახალ კომპიუტერზე Claude Code-ის გახსნის შემდეგ ჩასვი ეს:

> ეს არის ონლაინ მაღაზიის პროექტი Next.js-ზე, რომელიც წინა სესიაზე ავაშენეთ.
> წაიკითხე `HANDOFF.md` და `README.md` — იქ არის სრული კონტექსტი: რა გაკეთდა,
> რა გადაწყვეტილებები მივიღეთ და რა დარჩა. ამის შემდეგ დაველოდები დავალებას.

შემდეგ უთხარი, რისი გაკეთება გინდა. მაგალითად:

- „დაამატე შეკვეთის დადასტურების ელფოსტა Resend-ით"
- „დაამატე მომხმარებლის ანგარიშები — რეგისტრაცია, შესვლა, შეკვეთების ისტორია"
- „გადაიყვანე PostgreSQL-ზე და მოამზადე Vercel-ზე დასაპუშად"
- „დაამატე TBC-ის ეკვაირინგი BOG-ის გვერდით"
